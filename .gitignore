# Node modules (dependencies installed via npm/yarn)
node_modules/

# Build output (generated files after running `npm run build`)
dist/

# Local environment variables (API keys, secrets, etc.)
.env
.env.local
.env.development
.env.production

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
debug.log*
lerna-debug.log*

# Editor-specific files (VS Code, JetBrains, etc.)
.vscode/
.idea/
*.swp
*.swo
*.swn

# macOS system files
.DS_Store

# Windows system files
Thumbs.db
desktop.ini

# Optional: Ignore Netlify settings (if deploying manually)
.netlify/
